# Your Stellar wallet secret key (64-byte hex string)
# Used to derive stealth viewing/spending keys.
STELLAR_SECRET_KEY="your-64-byte-hex-secret-key-here"

# ISO timestamp or Unix seconds to scan announcements from (e.g. "2025-01-01T00:00:00Z")
# Leave empty to scan from the beginning.
FROM_TIMESTAMP=""
